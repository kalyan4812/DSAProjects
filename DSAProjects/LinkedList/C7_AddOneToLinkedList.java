package LinkedList;

public class C7_AddOneToLinkedList {

	/*public static Node addOne(Node head) {
		int k = fun(head);
		if (k == 1) {
			Node x = new Node(1);
			x.next = head;
			head = x;
		}
		return head;

	}

	public static int fun(Node head) {
		if (head == null)
			return 1;

		int k = head.data + fun(head.next);

		head.data = k % 10;

		return k / 10;
	}
	*/
}
